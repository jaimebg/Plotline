# Fase 6 — iPad nativo

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que Plotline se vea como una app de iPad en el dispositivo con el que App Review la rechazó.

**Architecture:** Rejillas que se adaptan por ancho disponible en lugar de dos columnas fijas, y un ancho de lectura acotado en las pantallas de texto. El trabajo se concentra en seis rejillas y la ficha; no se tocan las 89 vistas.

**Tech Stack:** SwiftUI, iOS 26, Swift Testing.

**Spec:** `docs/superpowers/specs/2026-08-01-app-store-4.2-design.md` §9.

## Por qué esta fase va antes que el "dónde verlo"

El rechazo dice el dispositivo: **iPad Air 11-inch (M3)**. Hasta el commit que abre esta fase, `TARGETED_DEVICE_FAMILY` era `1` — la app **no era una app de iPad**, así que el revisor la vio en modo compatibilidad, en una ventana de tamaño teléfono sobre una pantalla grande. En ese modo cualquier app parece escasa por mucho contenido que tenga, y todo lo construido en las Fases 4 y 5 le llegó encogido.

El iPad multiplica el trabajo ya hecho. Los proveedores de streaming añaden una fila más a una pantalla que quizá vio encajonada.

## Lo que ya está hecho y lo que se descubrió al mirar

`TARGETED_DEVICE_FAMILY = "1,2"` ya está aplicado (es la única edición de `project.pbxproj` que esta fase necesita, y ya está hecha). Con esa línea sola, verificado en el simulador de iPad Air 11":

- **Discover ya se ve bien.** Los estantes horizontales llenan el ancho de forma natural, muestran cinco o seis pósters, y la barra de pestañas adopta el aspecto nativo de iPad. No hace falta tocarlo.
- **La rejilla de géneros se rompe.** Dos franjas de 800 pt por fila y media pantalla vacía debajo.
- **La ficha se vacía.** La tarjeta de "Ratings" flota sola como una cajita en un ancho de 1640 pt, y el texto de Overview cruza la pantalla entera en líneas larguísimas.

`SUPPORTED_PLATFORMS` ya está recortado a iPhone desde la Fase 1, y las cuatro orientaciones de iPad ya están puestas en los ajustes de build. **De §9 solo queda el trabajo de layout.**

## Global Constraints

- Deployment target iOS 26.0. **Swift 5 language mode.** No intentar migración a Swift 6.
- **Nunca `.white` para texto** — `.primary` / `.secondary`. **Nunca fondos oscuros hardcodeados** — `Color.plotlineBackground` / `Color.plotlineCard`. Claro y oscuro.
- **Todo el texto de UI en inglés.**
- **Nunca editar `Plotline.xcodeproj/project.pbxproj` ni `Plotline/Info.plist`.** La única edición que esta fase necesitaba ya está hecha. Los `.swift` nuevos bajo `Plotline/` y `PlotlineTests/` entran solos en el target.
- No modificar `Plotline/Models/EpisodeMetric.swift`, `Plotline/Models/SeriesAnalysis.swift`, `Plotline/Models/PlotlineDataset.swift` ni `Plotline/Services/Analysis/SeriesAnalysisEngine.swift` — los comparte por symlink el generador de datasets y solo pueden importar `Foundation`.
- **Nada de esta fase puede empeorar el iPhone.** Es el dispositivo de la inmensa mayoría de usuarios y la app se ve bien ahí. Cada tarea se verifica en los dos.
- Commits en Conventional Commits, en inglés.
- Punto de partida: **120** tests de app, 48 de generador.

**Comandos:**

```bash
# iPhone
xcodebuild -project Plotline.xcodeproj -scheme Plotline \
  -destination 'platform=iOS Simulator,name=iPhone 17' test 2>/dev/null

# iPad — el dispositivo del rechazo
xcodebuild -project Plotline.xcodeproj -scheme Plotline \
  -destination 'platform=iOS Simulator,name=iPad Air 11-inch (M4)' -derivedDataPath build build
```

Contar con `grep -cE "Test case '.*' passed"` **sin ancla**. El recuento puede leerse uno corto porque el runner parte líneas y porque los tests parametrizados repiten nombre: lo que decide es `TEST SUCCEEDED` con cero fallos.

**Trampa de entorno:** `Application failed preflight checks` o `Busy` es el simulador, no el código. `xcrun simctl uninstall booted com.jbgsoft.Plotline` y repetir una vez. **Nunca cambiar código por ese error.**

---

## Estructura de archivos

| Archivo | Responsabilidad | Acción |
|---|---|---|
| `Plotline/Extensions/View+AdaptiveLayout.swift` | Columnas por ancho disponible y ancho de lectura acotado | Crear (Tarea 1) |
| `Plotline/Views/Discovery/GenreBrowseView.swift` | Rejilla de géneros | Modificar (Tarea 2) |
| `Plotline/Views/Discovery/GenreResultsView.swift` | Rejilla de resultados | Modificar (Tarea 2) |
| `Plotline/Views/Discovery/MoodSelectionView.swift` | Rejilla de estados de ánimo | Modificar (Tarea 2) |
| `Plotline/Views/Stats/StatsView.swift` | Tarjetas de resumen | Modificar (Tarea 2) |
| `Plotline/Views/Stats/TrendsView.swift` | Tarjetas de tendencias | Modificar (Tarea 2) |
| `Plotline/Views/Stats/CareerProfileView.swift` | Tarjetas de perfil | Modificar (Tarea 2) |
| `Plotline/Views/Detail/MediaDetailView.swift` | Ancho de lectura y orden de secciones | Modificar (Tarea 3) |
| `Plotline/Views/MainTabView.swift` | Barra lateral adaptable | Modificar (Tarea 4) |
| `PlotlineTests/AdaptiveLayoutTests.swift` | El cálculo de columnas | Crear (Tarea 1) |

---

## Task 1: Columnas por ancho, no por número

**Files:**
- Create: `Plotline/Extensions/View+AdaptiveLayout.swift`
- Create: `PlotlineTests/AdaptiveLayoutTests.swift`

**Interfaces:**
- Produces: `GridItem.adaptiveColumns(minimumWidth:spacing:) -> [GridItem]` y el modificador `.readableWidth(_ maximum: CGFloat)`

**El enfoque, y por qué no se ramifica por size class.** `GridItem(.adaptive(minimum:))` deja que SwiftUI meta tantas columnas como quepan con ese ancho mínimo. Un solo valor da 2 columnas en iPhone y 5 o 6 en iPad, sin condicionales, sin `horizontalSizeClass`, y funciona igual en multitarea con la app a media pantalla — que es donde una ramificación por size class se equivoca.

Ramificar por size class produciría "iPad = 4 columnas" y luego mostraría cuatro columnas apretadas cuando el usuario pone la app en un tercio de pantalla.

- [ ] **Step 1: Escribir los tests que fallan**

Crear `PlotlineTests/AdaptiveLayoutTests.swift`:

```swift
import SwiftUI
import Testing
@testable import Plotline

@Suite("Adaptive layout")
struct AdaptiveLayoutTests {
    /// The count SwiftUI will settle on for a given container width, mirrored
    /// here so the chosen minimums can be checked against real device widths
    /// without rendering anything.
    private func columnCount(width: CGFloat, minimum: CGFloat, spacing: CGFloat) -> Int {
        max(1, Int((width + spacing) / (minimum + spacing)))
    }

    @Test("poster grids give two columns on an iPhone and more on an iPad")
    func posterGridAdapts() {
        let minimum = AdaptiveLayout.posterMinimumWidth
        let spacing = AdaptiveLayout.gridSpacing

        // iPhone 17 portrait, minus the 16pt padding on each side.
        #expect(columnCount(width: 402 - 32, minimum: minimum, spacing: spacing) == 2)
        // iPad Air 11" portrait — the review device.
        #expect(columnCount(width: 820 - 32, minimum: minimum, spacing: spacing) >= 4)
    }

    @Test("banner grids stay readable rather than stretching")
    func bannerGridAdapts() {
        let minimum = AdaptiveLayout.bannerMinimumWidth
        let spacing = AdaptiveLayout.gridSpacing

        #expect(columnCount(width: 402 - 32, minimum: minimum, spacing: spacing) == 2)
        #expect(columnCount(width: 820 - 32, minimum: minimum, spacing: spacing) >= 3)
    }

    /// Multitasking is where a size-class branch would go wrong: an iPad in a
    /// one-third split is a compact width and must fall back to two columns.
    @Test("a narrow multitasking split falls back to two columns")
    func narrowSplitStaysCompact() {
        let minimum = AdaptiveLayout.posterMinimumWidth
        let spacing = AdaptiveLayout.gridSpacing

        #expect(columnCount(width: 320, minimum: minimum, spacing: spacing) <= 2)
    }

    @Test("the readable width does not constrain a phone")
    func readableWidthLeavesPhonesAlone() {
        #expect(AdaptiveLayout.readableMaximumWidth > 402)
    }
}
```

- [ ] **Step 2: Ejecutar para verificar que falla**

Run: el comando de tests de iPhone.
Expected: FAIL de compilación, `cannot find 'AdaptiveLayout' in scope`.

- [ ] **Step 3: Escribir el archivo**

Crear `Plotline/Extensions/View+AdaptiveLayout.swift`:

```swift
import SwiftUI

/// Layout values shared by every adaptive grid in the app.
///
/// Grids size themselves from the width they are given rather than from the
/// device or the size class. One minimum width yields two columns on a phone
/// and five or six on an iPad, and it stays right when the app is running in a
/// third of an iPad's screen — the case a size-class branch gets wrong, since
/// it would still call for four columns in a 320pt window.
enum AdaptiveLayout {
    /// Enough for a poster and its two lines of caption.
    static let posterMinimumWidth: CGFloat = 160

    /// Wide enough that a genre or mood banner still reads as a banner.
    static let bannerMinimumWidth: CGFloat = 220

    /// Statistics cards carry a number and a label.
    static let cardMinimumWidth: CGFloat = 180

    static let gridSpacing: CGFloat = 12

    /// Where a column of running text stops growing.
    ///
    /// Beyond roughly this width the eye loses the start of the next line. It
    /// sits well above any iPhone width so phones are never affected.
    static let readableMaximumWidth: CGFloat = 700
}

extension GridItem {
    /// Columns that fit the available width, rather than a fixed count.
    static func adaptiveColumns(
        minimumWidth: CGFloat,
        spacing: CGFloat = AdaptiveLayout.gridSpacing
    ) -> [GridItem] {
        [GridItem(.adaptive(minimum: minimumWidth), spacing: spacing)]
    }
}

extension View {
    /// Stops a column of text growing past a comfortable reading width and
    /// centres it in whatever space is left.
    ///
    /// A no-op on iPhone, where the screen is narrower than the maximum.
    func readableWidth(_ maximum: CGFloat = AdaptiveLayout.readableMaximumWidth) -> some View {
        frame(maxWidth: maximum, alignment: .leading)
            .frame(maxWidth: .infinity, alignment: .center)
    }
}
```

- [ ] **Step 4: Ejecutar los tests**

Run: el comando de tests de iPhone.
Expected: `TEST SUCCEEDED`, 0 fallos, **124** tests (120 + 4).

- [ ] **Step 5: Commit**

```bash
git add Plotline/Extensions/View+AdaptiveLayout.swift PlotlineTests/AdaptiveLayoutTests.swift
git commit -m "feat: add width-driven grid columns and a readable text width

Grids size themselves from the space they are given rather than from the
device, so they stay right in an iPad multitasking split — the case a
size-class branch gets wrong."
```

---

## Task 2: Las seis rejillas fijas

**Files:**
- Modify: `Plotline/Views/Discovery/GenreBrowseView.swift`
- Modify: `Plotline/Views/Discovery/GenreResultsView.swift`
- Modify: `Plotline/Views/Discovery/MoodSelectionView.swift`
- Modify: `Plotline/Views/Stats/StatsView.swift`
- Modify: `Plotline/Views/Stats/TrendsView.swift`
- Modify: `Plotline/Views/Stats/CareerProfileView.swift`

**Interfaces:**
- Consumes: `GridItem.adaptiveColumns(minimumWidth:spacing:)`, `AdaptiveLayout` (Tarea 1)

Las seis declaran dos `GridItem(.flexible())`. En un iPad de 1640 pt eso da columnas de 800 pt: la rejilla de géneros son dos franjas gigantes con media pantalla vacía debajo, verificado en el simulador.

**Qué mínimo usa cada una:**

| Vista | Contenido | Mínimo |
|---|---|---|
| `GenreBrowseView` | Banners de género | `AdaptiveLayout.bannerMinimumWidth` |
| `MoodSelectionView` | Banners de ánimo | `AdaptiveLayout.bannerMinimumWidth` |
| `GenreResultsView` | Pósters con título | `AdaptiveLayout.posterMinimumWidth` |
| `StatsView` (línea ~166) | Tarjetas de resumen | `AdaptiveLayout.cardMinimumWidth` |
| `TrendsView` (línea ~7) | Tarjetas de tendencia | `AdaptiveLayout.cardMinimumWidth` |
| `CareerProfileView` (línea ~216) | Tarjetas de perfil | `AdaptiveLayout.cardMinimumWidth` |

- [ ] **Step 1: Sustituir las declaraciones de columnas**

En las tres de `Discovery/`, que declaran una propiedad `private let columns`:

```swift
    private let columns = GridItem.adaptiveColumns(minimumWidth: AdaptiveLayout.bannerMinimumWidth)
```

(con `posterMinimumWidth` en `GenreResultsView`).

En las tres de `Stats/`, que declaran las columnas en línea dentro del `LazyVGrid`, sustituir el array literal por la llamada:

```swift
        LazyVGrid(columns: GridItem.adaptiveColumns(minimumWidth: AdaptiveLayout.cardMinimumWidth), spacing: 16) {
```

**Conserva el `spacing:` que cada `LazyVGrid` ya tenía** — varía entre vistas y no es objeto de esta tarea.

- [ ] **Step 2: Compilar para iPhone y para iPad**

Run los dos comandos de build.
Expected: `** BUILD SUCCEEDED **` en ambos.

- [ ] **Step 3: Ejecutar la suite**

Expected: `TEST SUCCEEDED`, 124 tests, sin cambio.

- [ ] **Step 4: Commit**

```bash
git add Plotline/Views/Discovery/GenreBrowseView.swift Plotline/Views/Discovery/GenreResultsView.swift Plotline/Views/Discovery/MoodSelectionView.swift Plotline/Views/Stats/StatsView.swift Plotline/Views/Stats/TrendsView.swift Plotline/Views/Stats/CareerProfileView.swift
git commit -m "fix: let every grid choose its own column count

Six grids asked for exactly two columns, which on an 11-inch iPad meant
800pt banners and half an empty screen."
```

---

## Task 3: La ficha en pantalla ancha

**Files:**
- Modify: `Plotline/Views/Detail/MediaDetailView.swift`

**Interfaces:**
- Consumes: `.readableWidth()` (Tarea 1)

**Lo que se ve hoy en iPad, verificado en el simulador:** bajo la imagen de cabecera, el bloque de contenido ocupa los 1640 pt completos. La tarjeta de "Ratings" queda como una cajita sola en la esquina izquierda con un vacío enorme a su derecha, y el párrafo de Overview cruza la pantalla entera en líneas que cuestan de seguir.

La cabecera y los estantes horizontales **sí** deben seguir usando todo el ancho: se ven bien y llenan la pantalla. Lo que se acota es la columna de texto y tarjetas.

- [ ] **Step 1: Acotar el bloque de contenido**

En `Plotline/Views/Detail/MediaDetailView.swift`, el `VStack(alignment: .leading, spacing: 24)` de la línea ~35 es el que contiene todas las secciones bajo la cabecera. Aplicarle el modificador al cierre de ese `VStack`, **después** de su `.padding()` existente si lo tiene:

```swift
                    .readableWidth()
```

**No** aplicarlo al `ScrollView` ni al `VStack(spacing: 0)` exterior: la imagen de cabecera debe seguir a sangre.

- [ ] **Step 2: Subir el análisis por encima de las recomendaciones**

Hoy "You Might Also Like" se renderiza antes que el bloque `if viewModel.isTVSeries`, así que el análisis propio de Plotline queda por debajo de un estante de recomendaciones de TMDB. Mover la sección de recomendaciones para que vaya **después** del bloque de series.

Es el argumento entero de la app frente al 4.2 y estaba por debajo de contenido ajeno. La ficha de películas no cambia de orden aparente, porque para ellas el bloque de series no renderiza nada.

- [ ] **Step 3: Compilar para iPhone y para iPad**

Expected: `** BUILD SUCCEEDED **` en ambos.

- [ ] **Step 4: Ejecutar la suite**

Expected: `TEST SUCCEEDED`, 124 tests, sin cambio.

- [ ] **Step 5: Commit**

```bash
git add Plotline/Views/Detail/MediaDetailView.swift
git commit -m "fix: keep the detail screen readable on a wide display

The content column ran the full 1640pt of an 11-inch iPad, leaving the
ratings card alone in a corner and the overview in lines too long to
follow. Plotline's own analysis also sat below a TMDB recommendation
shelf; it now comes first."
```

---

## Task 4: Barra lateral adaptable

**Files:**
- Modify: `Plotline/Views/MainTabView.swift`

**Interfaces:**
- Consumes: la `TabView(selection:)` existente con cinco `Tab(...)`

Con cinco secciones, `.tabViewStyle(.sidebarAdaptable)` es el patrón que Apple indica para iPad: da barra lateral en ancho regular y deja intacta la barra de pestañas del iPhone.

- [ ] **Step 1: Aplicar el estilo**

En `Plotline/Views/MainTabView.swift`, al `TabView(selection: $selectedTab)`, tras su llave de cierre:

```swift
        .tabViewStyle(.sidebarAdaptable)
```

- [ ] **Step 2: Compilar para iPhone y para iPad**

Expected: `** BUILD SUCCEEDED **` en ambos.

**Si el iPhone cambia de aspecto**, párate y dilo en el informe en vez de compensarlo: el modificador no debería alterar el compacto, y si lo hace hay que saberlo antes de seguir.

- [ ] **Step 3: Ejecutar la suite**

Expected: `TEST SUCCEEDED`, 124 tests, sin cambio.

- [ ] **Step 4: Commit**

```bash
git add Plotline/Views/MainTabView.swift
git commit -m "feat: use a sidebar on iPad and keep the tab bar on iPhone"
```

---

## Definición de terminado

- [ ] 124 tests pasando, `TEST SUCCEEDED`, cero fallos
- [ ] Compila para iPhone 17 y para iPad Air 11-inch
- [ ] Ninguna rejilla muestra dos columnas gigantes en iPad
- [ ] La ficha no deja la tarjeta de valoraciones sola en un ancho vacío
- [ ] El análisis de Plotline aparece antes que las recomendaciones
- [ ] **El iPhone no ha empeorado en ninguna pantalla** — verificado, no supuesto
- [ ] Verificado en claro y oscuro
- [ ] Ningún texto de UI en español

## Lo que esta fase deja fuera a propósito

- **`NavigationSplitView` en Favorites, Watchlist y Detail**, que pedía §9. Es una reestructuración grande de navegación con riesgo real de romper el iPhone, y las tres rejillas adaptables más el ancho de lectura resuelven la impresión de app estirada con una fracción del riesgo. Se reevalúa después de ver el resultado.
- **Alturas de gráfico por size class.** Los 200 y 300 pt fijos de `Stats/` se ven aceptables en iPad; no hay evidencia de que molesten.
- **Capturas de iPad para App Store Connect**, que §9 también pide. Van cuando el layout esté aprobado, no antes.

## Nota para la fase siguiente

Queda el "dónde verlo" del spec §7, con sus dos correcciones ya registradas: TMDB no devuelve deep links por plataforma, y la atribución a JustWatch se exige en cada ficha bajo pena de revocar el acceso a la API.
